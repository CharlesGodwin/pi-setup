# Run these steps manually to get started
sudo apt update
sudo apt upgrade -y
sudo raspi-config
# then these ones
sudo apt install -y git
git clone https://github.com/CharlesGodwin/pi-setup.git ~/pi-setup
cd ~/pi-setup
